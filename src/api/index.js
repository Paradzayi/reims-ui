/*
  In this file we expose a number of apis that aour app provies.
  It serves as an encapsulation to the HTTP requests that are
    made to the Reims api.
*/

import stands from './stands'

export default {
  stands
}
